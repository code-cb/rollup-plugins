{
  "name": "@codecb/rollup-plugin-json",
  "version": "0.0.1",
  "license": "MIT",
  "repository": {
    "directory": "packages/url",
    "type": "git",
    "url": "https://github.com/code-cb/rollup-plugins.git"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@rollup/pluginutils": "^5.0"
  }
}
